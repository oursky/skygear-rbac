package constants

// IsDomain Assigning child domain to parent domain is like
// assigning role to user, except `domain` is constants.IsDomain
const IsDomain = "__domain__"

// NoSubject For declaring role under domain without actually user
const NoSubject = "__nosubject__"
